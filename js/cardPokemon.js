class Pokemon {
  constructor(name, type, abilities) {
    this.name = name;
    this.type = type;
    this.abilities = abilities;
  }
  createHTML() {
    const cardHTML = ``;

    return cardHTML;
  }
}
